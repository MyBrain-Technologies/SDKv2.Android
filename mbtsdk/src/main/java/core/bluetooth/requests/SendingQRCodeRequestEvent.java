package core.bluetooth.requests;

/**
 * An event class when a headset does not know its own QR Code so the mobile device sends it.
 */
public class SendingQRCodeRequestEvent extends BluetoothRequests {

        public SendingQRCodeRequestEvent(){

        }

}